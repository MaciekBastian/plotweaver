import 'package:flutter/material.dart';

class BlankEditorTab extends StatelessWidget {
  const BlankEditorTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('blank'),
    );
  }
}
